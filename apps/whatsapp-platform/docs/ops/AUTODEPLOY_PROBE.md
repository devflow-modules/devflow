# Autodeploy probe

Controlled documentation-only change to validate Git → Vercel Production
auto-deploy for `devflow-whatsapp` after Git reconnect (2026-08-03).

No runtime, schema, env, or UI impact.
