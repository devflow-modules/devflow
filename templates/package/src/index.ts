/**
 * @devflow/package-name
 *
 * Shared package — only reusable utilities, adapters, types, or helpers.
 * See docs/PACKAGE_GUIDELINES.md and docs/ARCHITECTURE_BOUNDARIES.md.
 *
 * Allowed: utilities, provider adapters, UI primitives, shared types,
 *          infrastructure helpers, analytics contracts, testing helpers.
 * Forbidden: product business logic, DB queries, product-specific plans/APIs/dashboards.
 */

export const PACKAGE_VERSION = "0.0.1";
