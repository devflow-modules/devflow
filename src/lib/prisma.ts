/**
 * Prisma client do WhatsApp Platform (WhatsappPhoneNumber, Tenant, etc).
 * Usado pelos módulos whatsapp-platform quando rodando no app raiz.
 */
export { prismaWhatsapp as prisma } from "./prisma-whatsapp";
