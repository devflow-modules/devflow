# @devflow/whatsapp-core

Package compartilhado para integração com a WhatsApp Cloud API (envio, webhooks, templates). Usado pelo app `apps/whatsapp-platform` e por outros produtos que precisem de utilitários WhatsApp.

A ser preenchido quando o WhatsApp Platform for implementado.
